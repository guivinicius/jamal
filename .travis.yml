language: ruby
rvm:
  - 2.0.0
  - 2.1.0
env:
  - DB=sqlite
  - DB=mysql
  - DB=postgresql
script:
  - RAILS_ENV=test bundle exec rake --trace db:migrate
  - bundle exec rspec spec
before_script:
  - mysql -e 'create database jamal_test'
  - psql -c 'create database jamal_test' -U postgres